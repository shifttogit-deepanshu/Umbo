import React from "react";

const BlackContainer = (props)=>{
    return (
        <div className="blackContainer-container">
            {props.children}
        </div>
    )
}

export default BlackContainer